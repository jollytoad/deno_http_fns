{
  "tasks": {
    "example": "deno run -A --import-map=examples/import_map.json",
    "check": "deno fmt && deno lint && deno check --import-map=examples/import_map.json **/*.ts"
  },
  "compilerOptions": {
    "verbatimModuleSyntax": true
  }
}
